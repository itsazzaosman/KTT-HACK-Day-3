# 📦 Footprint Report: AI Math Tutor (S2.T3.1)

## 1. Live Footprint Audit

Per the technical constraints, the total on-device footprint must remain **≤ 75 MB** (excluding the TTS cache).

Running the standard audit command on the directory yields:

```bash
$ du -sh --exclude=tts .
47M     .
```
